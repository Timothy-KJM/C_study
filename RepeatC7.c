/*#include <stdio.h>

int main(void) {
	int sum = 0;
	int i;

	printf("정수를 입력하세요: ");
	scanf_s("%d", &i);
	sum += i;
	while (i != 0) {
		if (i != 0) {
			printf("정수를 입력하세요: ");
			scanf_s("%d", &i);
			sum += i;
		}
		else {
			break;
		}
	}
	printf("%d", sum);
	return 0;
}*/